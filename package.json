{
  "name": "puppeteer-scraper",
  "version": "1.0.0",
  "description": "Puppeteer Price Scraper",
  "main": "server.js",
  "dependencies": {
    "express": "^4.17.3",
    "puppeteer": "^21.0.0"
  },
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": "18"
  }

  "engines": {
  "node": "18"
},
"scripts": {
  "start": "node server.js",
  "install": "corepack enable && corepack prepare pnpm@latest --activate && pnpm install"
}

}
